// Set this to a real GA4 measurement ID (e.g. "G-XXXXXXXXXX") to enable
// Google Analytics. Left null, analytics stays off entirely, so a freshly
// cloned template never reports fake traffic to anyone.
export const GA_MEASUREMENT_ID: string | null = null;
